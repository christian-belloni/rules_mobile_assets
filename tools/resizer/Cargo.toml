[workspace]

[package]
name = "resizer"
version = "0.1.0"
edition = "2021"

[dependencies]
resvg = "0.43.0"
clap = { version = "4.5", features = ["derive"] }
image-webp = "0.2.4"
image = { version = "0.25.10", default-features = false, features = ["png", "jpeg", "ico", "tiff", "webp"] }
